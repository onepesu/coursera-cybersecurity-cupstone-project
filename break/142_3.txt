This program does not recognise that a logread with the wrong token is an
integrity violation. Specifically, in such a command it respond with invalid and 255 while the oracle prints
integrity violation.